#ifndef FCN_H_       //wenn diese Header noch nicht definiert ist, dann:
#define FCN_H_	    //...gilt das hier

double fcn(double);   //wir schreiben nur was es ist (also der Rückgabewert)(double) wie sie heißt (fnc) und welchen wert sie erwartet (double)
double der(double);
double nfc(double, double, double); //hier sind 3 double benötigt, da er 3 Werte erwartet

#endif					//und hier endet es dann
